import React, { useEffect } from 'react'\n\ntype AdSlotProps = { client?: string; slot?: string; style?: React.CSSProperties; className?: string; devPlaceholder?: boolean; width?: number; height?: number }\n\nexport default function AdSlot({ client, slot, style, className, devPlaceholder = true, width = 320, height = 100 }: AdSlotProps) {\n  const adsenseClient = client || process.env.NEXT_PUBLIC_ADSENSE_CLIENT\n  const adsenseSlot = slot || process.env.NEXT_PUBLIC_ADSENSE_SLOT\n  const isProd = process.env.NODE_ENV === 'production'\n\n  useEffect(() => {\n    if (!isProd) return\n    if (!adsenseClient) return\n    if ((window as any).adsbygoogleLoaded) return\n    const s = document.createElement('script')\n    s.async = true\n    s.src = `https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=${adsenseClient}`\n    s.crossOrigin = 'anonymous'\n    document.head.appendChild(s)\n    ;(window as any).adsbygoogleLoaded = true\n  }, [adsenseClient, isProd])\n\n  if (!isProd && devPlaceholder) {\n    return (\n      <div className={className} style={{ border: '2px dashed #ccc', display: 'inline-flex', alignItems: 'center', justifyContent: 'center', width, height, color: '#666', ...style }}>\n        Ad placeholder\n      </div>\n    )\n  }\n\n  return <ins className="adsbygoogle" style={{ display: 'inline-block', width: `${width}px`, height: `${height}px`, ...style }} data-ad-client={adsenseClient} data-ad-slot={adsenseSlot} data-ad-format="auto" data-full-width-responsive="true" />\n}